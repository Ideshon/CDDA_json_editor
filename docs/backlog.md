# Backlog

Приоритеты синхронизируются с `status_worker.json`.

## Active

- Фаза 6: drag-and-drop порядка полей с сохранением порядка в JSON.

## Planned

- Фаза 7: аудит справок по полям и пользовательские overrides.
- Фаза 8: пользовательские расширения схем и категорий.
- Фаза 9: отдельное окно редактирования диалогов.
- Фаза 10: горячие клавиши и настройка сочетаний клавиш.

## Low Priority

- Расширенная техническая документация сверх минимального набора в `docs/`.

## Completed

- Фаза 1: показывать файл выбранного объекта и добавить индексы объектов/связей.
- Фаза 2: перемещение одного или нескольких объектов между JSON-файлами.
- Фаза 3: переименование объектов с предупреждением и обновлением найденных `ref_list` ссылок.
- Фаза 4: создавать бэкап мода при открытии и дать кнопку восстановления бэкапа.
- Фаза 4: автобэкап во время работы с возможностью отключения и настройкой интервала.
- Фаза 4: запоминание состояния окна и логирование в консоль/файл.
- Фаза 4: кнопки назад/вперед для отмены и повтора действий редактора.
- Фаза 5: неблокирующее окно связей выбранного объекта с динамическим переключением и переходом к объектам из списка связей.
